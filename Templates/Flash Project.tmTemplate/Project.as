package ${TM_FILE_PACKAGE}
{

	import flash.events.Event;
	import flash.display.Sprite;

 	/**
 	 * Application entry point for ${TM_NEW_FILE_BASENAME}.
 	 * 
 	 * @langversion ActionScript 3.0
 	 * @playerversion Flash 9.0
 	 * 
 	 * @author ${TM_FULLNAME}
 	 * @since ${TM_DATE}
	 * @copyright ${TM_ORGANIZATION_NAME}-${TM_YEAR}
 	 */

	//---------------------------------------
	// !!REMOVE ME!!
	//---------------------------------------
	// Document Class: ${TM_FILE_PACKAGE}.${TM_NEW_FILE_BASENAME}
	// Build Path: ../deploy/swf/${TM_NEW_FILE_BASENAME}.swf
	//---------------------------------------

	public class ${TM_NEW_FILE_BASENAME} extends Sprite
	{
	
		public function ${TM_NEW_FILE_BASENAME}()
		{
			trace("${TM_NEW_FILE_BASENAME}::${TM_NEW_FILE_BASENAME}()",  new Date().toString() );
			
		}
	
	
	}

}
